spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/salves/code/microservices-demo/config-server-repository
          clone-on-start: true
    fail-fast: true